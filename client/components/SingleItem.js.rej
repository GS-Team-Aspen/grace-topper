diff a/client/components/SingleItem.js b/client/components/SingleItem.js	(rejected hunks)
@@ -24,11 +24,10 @@ class SingleItem extends Component {
-          <SingleItemDetails
-            {...this.props.item}
-            review={reviews}
-            add={quantity =>
-              this.props.addCart(
-                this.props.item.id,
-                this.props.orderId,
-                quantity
-              )}
-	      remove={() => this.props.deleteItem(this.props.item.id, this.props.history)}
-          />
+        <SingleItemDetails
+          {...this.props.item}
+          review={reviews}
+          add={quantity =>
+            this.props.addCart(this.props.item.id, this.props.orderId, quantity)
+          }
+          remove={() =>
+            this.props.deleteItem(this.props.item.id, this.props.history)
+          }
+        />
@@ -36 +35 @@ class SingleItem extends Component {
-          <ReviewWrap {...reviews} />
+        <ReviewWrap {...reviews} />
@@ -55,2 +54,2 @@ const mapDispatchToProps = dispatch => {
-	dispatch(addToCart(itemId, orderId, quantity)),
-      deleteItem: (itemId, history) => dispatch(removeItem(itemId, history))
+      dispatch(addToCart(itemId, orderId, quantity)),
+    deleteItem: (itemId, history) => dispatch(removeItem(itemId, history))
