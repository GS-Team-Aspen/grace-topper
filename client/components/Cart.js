import React from 'react'
import {connect} from 'react-redux'

const Cart = () => <div>Hello World</div>

const mapState = state => ({})

const mapDispatch = dispatch => ({})

export default connect(mapState, mapDispatch)(Cart)
